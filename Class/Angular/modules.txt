before angular 15 weaks module now a days we are using angular 21 version 
so modules are optional. We can now use standalerts componenets

services:
    its like a helper class
    used for store data or share logic that many components are used.
    instead of writing same code in every component we write it once in a service
ng g s service-name
